name: 🐧 Ubuntu Hardened Image Build

on:
  workflow_dispatch:

jobs:
  build-ubuntu:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: write

    env:
      OS_NAME: "ubuntu"
      OS_VERSION: "22.04"
      CIS_VERSION: "1.4"
      AWS_REGION: "ap-south-1"
      ACCOUNT_ID: "661539128717"
      PACKER_VERSION: "1.14.2"

    steps:
      # 1️⃣ Checkout Code
      - name: 🧾 Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Configure AWS credentials (OIDC)
      - name: 🔐 Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ env.ACCOUNT_ID }}:role/GitHubActionsFactoryRole
          aws-region: ${{ env.AWS_REGION }}

      # 3️⃣ Base dependencies
      - name: 🔧 Install dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y wget curl unzip jq python3-pip

      # 4️⃣ Install packer
      - name: 📦 Install Packer
        run: |
          curl -fsSL "https://releases.hashicorp.com/packer/${PACKER_VERSION}/packer_${PACKER_VERSION}_linux_amd64.zip" -o packer.zip
          sudo unzip -o packer.zip -d /usr/local/bin/
          rm -f packer.zip
          packer version

      # 5️⃣ Initialize and validate packer
      - name: 🧩 Initialize and validate Packer
        run: |
          packer init packer/ubuntu
          packer validate packer/ubuntu

      # 6️⃣ Build hardened Ubuntu image
      - name: 🏗️ Build hardened Ubuntu image
        run: |
          DATE=$(date +%Y%m%d)
          COMMIT=$(echo "${GITHUB_SHA}" | cut -c1-7)
          BASE_TAG="${{ env.OS_NAME }}-${{ env.OS_VERSION }}-cis${{ env.CIS_VERSION }}"
          IMAGE_NAME="${BASE_TAG}-hardening-${DATE}"
          echo "Building image: ${IMAGE_NAME}"
          SOURCE_AMI="ami-05a8df10b57fXXXX"
          packer build \
            -var "image_name=${IMAGE_NAME}" \
            -var "base_image=ubuntu:${{ env.OS_VERSION }}" \
            -var "cis_profile=cis${{ env.CIS_VERSION }}" \
            -var "build_date=${DATE}" \
            -var "build_time=${TIME}" \
            -var "git_commit=${COMMIT}" \
            -var "enterprise_tag=${{ env.ENTERPRISE_TAG }}" \
            -var "source_ami=${SOURCE_AMI}" \
            -var "description=${{ env.ENTERPRISE_TAG }} built using source AMI ${SOURCE_AMI}" \
            packer/ubuntu

          echo "DATE=${DATE}" >> $GITHUB_ENV
          echo "COMMIT=${COMMIT}" >> $GITHUB_ENV
          echo "BASE_TAG=${BASE_TAG}" >> $GITHUB_ENV

      - name: 🧱 Tag and push hardened image to ECR (immutable with datetime)
        run: |
          # Generate timestamped version tag
          DATE=$(date +%Y%m%d)
          TIME=$(date +%H%M%S)
          COMMIT=${{ env.COMMIT }}
          BASE_TAG=${{ env.BASE_TAG }}
          FULL_TAG="${BASE_TAG}-${DATE}${TIME}-${COMMIT}"

          echo "🔹 Generated immutable tag: ${FULL_TAG}"

          aws ecr get-login-password --region ${{ env.AWS_REGION }} \
            | docker login --username AWS \
              --password-stdin ${{ env.ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com

          docker tag ${BASE_TAG}-hardening-${DATE}:latest \
            ${{ env.ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/hardened-${{ env.OS_NAME }}:${FULL_TAG}

          docker push ${{ env.ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/hardened-${{ env.OS_NAME }}:${FULL_TAG}

          echo "FULL_TAG=${FULL_TAG}" >> $GITHUB_ENV
          echo "✅ Image pushed successfully with timestamped immutable tag"


      # 8️⃣ Generate enterprise tag (vendor-version-intuit-iteration)
      - name: 🏷️ Generate enterprise tag
        id: taggen
        run: |
          VENDOR="${{ env.OS_NAME }}"
          VERSION="${{ env.OS_VERSION }}"
          VARIANT="intuit"
          ITERATION="1"
          ENTERPRISE_TAG="${VENDOR}-${VERSION}-${VARIANT}-${ITERATION}"
          echo "ENTERPRISE_TAG=${ENTERPRISE_TAG}" >> $GITHUB_ENV
          echo "Enterprise tag generated: ${ENTERPRISE_TAG}"


      - name: 🏷️ Generate enterprise tag (semantic 1.x versioning)
        id: taggen
        run: |
          VENDOR="${{ env.OS_NAME }}"
          VERSION="${{ env.OS_VERSION }}"
          VARIANT="intuit"

          echo "🔍 Fetching existing tags for hardened-${VENDOR}..."
          EXISTING_TAGS=$(aws ecr list-images \
            --repository-name hardened-${VENDOR} \
            --region ${{ env.AWS_REGION }} \
            --query "imageIds[?starts_with(imageTag, \`${VENDOR}-${VERSION}-${VARIANT}\`)].imageTag" \
            --output text)

          echo "🧾 Existing tags: $EXISTING_TAGS"

          # Default to 1.0 if no tags exist
          BASE_VERSION="1.0"
          if [ -z "$EXISTING_TAGS" ]; then
            NEW_VERSION=$BASE_VERSION
          else
            # Extract numeric parts from tags like ubuntu-22.04-intuit-1.3
            LAST_TAG=$(echo "$EXISTING_TAGS" | tr '\t' '\n' | sort -V | tail -n 1)
            LAST_VERSION=$(echo "$LAST_TAG" | awk -F '-' '{print $NF}')
            echo "🔢 Last semantic version found: $LAST_VERSION"

            # Increment the minor version by 0.1
            MAJOR=$(echo "$LAST_VERSION" | cut -d. -f1)
            MINOR=$(echo "$LAST_VERSION" | cut -d. -f2)
            NEXT_MINOR=$((MINOR + 1))
            NEW_VERSION="${MAJOR}.${NEXT_MINOR}"
          fi

          ENTERPRISE_TAG="${VENDOR}-${VERSION}-${VARIANT}-${NEW_VERSION}"

          echo "🚀 New enterprise tag: ${ENTERPRISE_TAG}"
          echo "ENTERPRISE_TAG=${ENTERPRISE_TAG}" >> $GITHUB_ENV


      # 🔟 Upload compliance reports to S3 and trigger AWS Glue crawler
      - name: ☁️ Upload reports to S3 and trigger Glue
        env:
          AWS_REGION: ${{ env.AWS_REGION }}
        run: |
          echo "☁️ Uploading reports for ${{ env.OS_NAME }} to S3 and triggering Glue crawler..."

          # make sure Python deps exist
          pip3 install boto3

          # call your existing script
          python3 tools/upload_to_s3_and_trigger_glue.py \
            --bucket os-compliance-reports \
            --region ${{ env.AWS_REGION }} \
            --os ${{ env.OS_NAME }} \
            --build_date $(date +%Y%m%d)
