name: 🐧 Ubuntu Hardened Image Build

on:
  workflow_dispatch:

jobs:
  build-ubuntu:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: write

    env:
      OS_NAME: "ubuntu"
      OS_VERSION: "22.04"
      CIS_VERSION: "1.4"
      AWS_REGION: "ap-south-1"
      PACKER_VERSION: "1.14.2"

    steps:
      - name: 🧾 Checkout repository
        uses: actions/checkout@v4

      - name: 🔐 Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::661539128717:role/GitHubActionsFactoryRole
          aws-region: ${{ env.AWS_REGION }}

      - name: 🧠 Debug existing containerd packages
        run: |
          echo "=== Installed container-related packages ==="
          dpkg -l | grep -E "docker|containerd" || echo "No containerd/docker packages found"

      - name: 🔧 Install dependencies (no containerd conflicts)
        run: |
          set -e
          echo "🔧 Installing dependencies safely..."
          sudo apt-mark hold docker-ce docker-ce-cli containerd.io containerd || true
          sudo apt-get update -y
          sudo apt-get install -y wget curl unzip python3-pip gpg lsb-release jq software-properties-common
          echo "✅ Base dependencies installed"

      - name: 📦 Install Packer (binary method - conflict-free)
        run: |
          echo "📦 Installing Packer ${PACKER_VERSION} via binary..."
          curl -fsSL "https://releases.hashicorp.com/packer/${PACKER_VERSION}/packer_${PACKER_VERSION}_linux_amd64.zip" -o packer.zip
          sudo unzip -o packer.zip -d /usr/local/bin/
          rm -f packer.zip
          packer version
          echo "✅ Packer installed successfully via binary"

      - name: 🧩 Install required Packer plugins
        run: |
          echo "📦 Installing Packer plugins..."
          packer plugins install github.com/hashicorp/docker
          packer plugins install github.com/hashicorp/ansible
          echo "✅ Plugins installed successfully"

      - name: 🔍 Install Trivy scanner
        run: |
          echo "🔍 Installing Trivy..."
          curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh | sh -s -- -b /tmp
          sudo mv /tmp/trivy /usr/local/bin/trivy
          trivy --version
          echo "✅ Trivy installed successfully"

      - name: 🧩 Initialize and validate Packer
        run: |
          echo "🧩 Initializing Packer configuration..."
          packer init packer/ubuntu
          packer validate packer/ubuntu
          echo "✅ Packer configuration is valid"

      - name: 🏗️ Build hardened Ubuntu image
        run: |
          TIMESTAMP=$(date +%Y%m%d)
          TAG="${{ env.OS_NAME }}-${{ env.OS_VERSION }}-cis${{ env.CIS_VERSION }}-hardening-${TIMESTAMP}"
          echo "🚀 Building hardened image: $TAG"
          packer build \
            -var "local_tag=latest" \
            -var "image_name=${TAG}" \
            -var "base_image=ubuntu:22.04" \
            packer/ubuntu
          echo "✅ Image built successfully: ${TAG}:latest"

      - name: 🔍 Scan image using Trivy
        run: |
          TIMESTAMP=$(date +%Y%m%d)
          TAG="${{ env.OS_NAME }}-${{ env.OS_VERSION }}-cis${{ env.CIS_VERSION }}-hardening-${TIMESTAMP}"
          mkdir -p reports
          trivy image "${TAG}:latest" --format json -o reports/trivy-${TAG}.json --severity HIGH,CRITICAL || true
          echo "✅ Security scan completed"

      - name: 📊 Upload scan reports as artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: security-reports
          path: reports/
          retention-days: 30
