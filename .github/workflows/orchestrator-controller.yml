name: 🧩 OS Hardening Controller

on:
  workflow_dispatch:
    inputs:
      vendor:
        description: "Choose target vendor (amazonlinux | ubuntu | rhel | all)"
        required: true
        default: "all"

# Prevents parallel runs from colliding
concurrency:
  group: "controller-${{ github.ref }}"
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  build-all:
    if: ${{ github.event.inputs.vendor == 'all' }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        vendor: [ubuntu, amazonlinux, rhel]
    steps:
      - name: ▶️ Trigger vendor workflow - ${{ matrix.vendor }}
        uses: ./.github/workflows/build-${{ matrix.vendor }}.yml

  build-one:
    if: ${{ github.event.inputs.vendor != 'all' }}
    runs-on: ubuntu-latest
    steps:
      - name: ▶️ Trigger vendor workflow - ${{ github.event.inputs.vendor }}
        uses: ./.github/workflows/build-${{ github.event.inputs.vendor }}.yml
