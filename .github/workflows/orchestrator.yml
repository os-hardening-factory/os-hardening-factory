name: 🏭 OS Hardening Orchestrator

on:
  workflow_dispatch:
    inputs:
      vendor:
        description: "Select OS vendor (amazonlinux, ubuntu, rhel, or all)"
        required: true
        default: "all"

concurrency:
  group: hardening-orchestrator
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    strategy:
      matrix:
        os_type: ${{ fromJson(github.event.inputs.vendor == 'all' && '["amazonlinux","ubuntu","rhel"]' || format('["{0}"]', github.event.inputs.vendor)) }}
    permissions:
      id-token: write
      contents: write

    steps:
      - name: 🧾 Checkout orchestrator repo
        uses: actions/checkout@v4

      - name: 🔐 Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::661539128717:role/GitHubActionsFactoryRole
          aws-region: ap-south-1

      - name: 🔎 Verify AWS identity
        run: aws sts get-caller-identity

      - name: 🔧 Install dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y wget curl gpg python3-pip software-properties-common --no-install-recommends
          wget -O- https://apt.releases.hashicorp.com/gpg | gpg --dearmor | sudo tee /usr/share/keyrings/hashicorp-archive-keyring.gpg >/dev/null
          echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
          sudo apt-get update -y
          sudo apt-get install -y packer
          pip3 install boto3 PyGithub
          echo "✅ Dependencies installed."

      - name: 🧩 Install required Packer plugins
        run: |
          packer plugins install github.com/hashicorp/ansible
          packer plugins install github.com/hashicorp/docker
          packer plugins list

      - name: 🏗️ Build hardened image for ${{ matrix.os_type }}
        run: |
          echo "🚀 Building hardened image for ${{ matrix.os_type }}..."
          packer init packer/${{ matrix.os_type }}
          packer validate -var-file=packer/variables.pkr.hcl packer/${{ matrix.os_type }}
          packer build -var-file=packer/variables.pkr.hcl \
                       -var "local_tag=${{ matrix.os_type }}-hardened:v1.0.0-cis1.4-$(date +%Y%m%d)-${GITHUB_SHA::7}" \
                       packer/${{ matrix.os_type }}
        timeout-minutes: 45

      - name: 🔍 Scan image using Trivy
        run: |
          echo "🔎 Scanning image..."
          trivy image 661539128717.dkr.ecr.ap-south-1.amazonaws.com/hardened-${{ matrix.os_type }}:latest -f json -o reports/trivy-${{ matrix.os_type }}.json || true

      - name: ☁️ Upload compliance report to S3
        run: python3 tools/upload_to_s3_and_trigger_glue.py
        env:
          AWS_REGION: ap-south-1

      - name: 🏷️ Update vendor repo changelog and release
        run: |
          python3 tools/dispatch_to_vendor_repos.py \
            --vendor ${{ matrix.os_type }} \
            --cis_profile "cis1.4" \
            --version "v1.0.0" \
            --build_date "$(date +%Y%m%d)" \
            --summary "CIS 1.4 compliance, CVE patches, and OS security updates"
        env:
          GITHUB_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
